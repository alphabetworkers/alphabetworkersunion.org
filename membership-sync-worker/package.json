{
  "private": true,
  "name": "membership-sync-worker",
  "version": "1.0.0",
  "description": "A worker that receives Stripe webhooks and writes new card signs to Airtable",
  "module": "index.mjs",
  "scripts": {
    "format": "prettier --write '**/*.{ts,js,mjs,css,json,md}'",
    "start": "wrangler dev",
    "deploy": "wrangler publish"
  },
  "devDependencies": {
    "prettier": "^1.18.2",
    "wrangler": "^2.0.9"
  }
}
